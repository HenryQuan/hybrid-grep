name: Release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g., v0.1.0)'
        required: true

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install MinGW (Windows)
        if: runner.os == 'Windows'
        run: choco install mingw -y --no-progress
        shell: bash

      - name: Build
        run: |
          case "${{ runner.os }}" in
            Linux)   out="hygp-linux"   ;;
            macOS)   out="hygp-macos"   ;;
            Windows) out="hygp-windows.exe" ;;
          esac
          gcc -O2 -s -o "$out" hygp.c
        shell: bash

      - uses: actions/upload-artifact@v4
        with:
          name: hygp-${{ runner.os }}
          path: hygp-*

  release:
    needs: [build]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          pattern: hygp-*
          merge-multiple: true

      - name: Create draft release
        run: |
          gh release create "${{ github.event.inputs.tag }}" \
            --draft --generate-notes \
            hygp-linux hygp-macos hygp-windows.exe
        env:
          GH_TOKEN: ${{ github.token }}
